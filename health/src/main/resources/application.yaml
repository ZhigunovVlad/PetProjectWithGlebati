server:
  port: 8081
  servlet:
    context-path: /health

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/TestDb
    username: testDb
    password: pass
  jpa:
    hibernate:
      ddl-auto: update # автообновление схемы
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect